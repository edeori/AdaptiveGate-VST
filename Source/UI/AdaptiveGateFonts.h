#pragma once
#include <juce_graphics/juce_graphics.h>

namespace adaptivegate::ui::Fonts
{
    juce::Font title (float height);
    juce::Font light (float height);
    juce::Font medium (float height);
    juce::Font numeric (float height);
}
